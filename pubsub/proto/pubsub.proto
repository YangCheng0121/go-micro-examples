syntax = "proto3";

package pubsub;

option go_package = "proto;helloworld";

// Example message
message Event {
	// unique id
	string id = 1;
	// unix timestamp
	int64 timestamp = 2;
	// message
	string message = 3;
}
